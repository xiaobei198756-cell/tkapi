# TODO

- Rotate any API keys that were previously stored in local `.env` files.
- Move Render production storage from SQLite to persistent disk or PostgreSQL.
- Implement TikTok Research API keyword search after official permission approval.
- Extend X search with media expansions and media fields.
- Implement Instagram Graph API collection for approved Business/Creator accounts.
- Implement Facebook Graph API collection for approved Page/public content access.
- Add authentication/authorization before destructive API routes.
- Add backend tests for `/api/search/keywords` across all platform statuses.
- Add frontend navigation tests for `/keywords`, `/dashboard`, and ranking routes.
